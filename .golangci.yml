version: "2"

linters:
  default: standard
  settings:
    errcheck:
      exclude-functions:
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
        - (*database/sql.Rows).Close
        - (*os.File).Close
        - os.RemoveAll

formatters:
  enable:
    - gofmt
    - goimports
